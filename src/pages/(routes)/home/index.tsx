import styles from "./index.module.scss";

const Home = () => {
  return (
    <div className={styles.wrapper}>
      <h1>Home</h1>
      <button>ChangeTheme</button>
    </div>
  );
};

export default Home;
